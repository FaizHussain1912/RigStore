const { PrismaClient } = require("@prisma/client"); const prisma = new PrismaClient(); async function main() { console.log("Adding columns..."); try { await prisma.$executeRawUnsafe("ALTER TABLE \"Order\" ADD COLUMN \"cancelRequested\" BOOLEAN NOT NULL DEFAULT false;"); await prisma.$executeRawUnsafe("ALTER TABLE \"Order\" ADD COLUMN \"cancelReason\" TEXT;"); console.log("Columns added successfully!"); } catch (e) { console.error("Error adding columns:", e); } finally { await prisma.$disconnect(); } } main();
