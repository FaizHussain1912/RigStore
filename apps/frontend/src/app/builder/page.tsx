import BuilderClient from './BuilderClient';

export const metadata = {
  title: 'Custom PC Builder | RigStore',
  description: 'Build your dream PC with our constraint-satisfaction compatibility engine.',
};

export default function BuilderPage() {
  return <BuilderClient />;
}
